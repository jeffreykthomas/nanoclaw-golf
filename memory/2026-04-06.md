- Linked `personal-golf` to the shared NanoClaw instruction and memory system via symlinks.
- Product direction: treat UI users like developers. User interactions may require app/codebase updates, with a likely future workflow of spawning a coding agent to make changes on a branch and open a PR for manual approval.
- Reference pattern found in `nanoclaw-mentors`: skill/update flows are git-native and branch-based rather than a custom skills engine.
